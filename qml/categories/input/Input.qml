import QtQuick 2.2
import ".."

Category {
    Options_Open {
        id: open
        width: parent.width
        expanded: true
    }
    Options_Arithmetic {
        width: parent.width
    }
    Options_FourierTransform {
        width: parent.width
    }
}
